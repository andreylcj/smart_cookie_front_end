# Run Application
```bash
# Clone this repo
git clone https://github.com/andreylcj/smart_cookie_front_end.git

# Access project folder
cd smart_cookie_front_end

# Install dependencies
npm install

# Execute application
npm run start

# The projetc will be visible on port:3000 - access on <http://localhost:3000>
```